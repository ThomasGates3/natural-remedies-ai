# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
# NEVER commit the actual key to git - use .env.local for local development
# For production, use GCP Secret Manager (see deployment docs)
VITE_GEMINI_API_KEY=your-api-key-here
